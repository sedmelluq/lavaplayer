bintray {
  user = project.findProperty('bintray.user')
  key = project.findProperty('bintray.apiKey')
  publications = ['mavenJava']
  publish = true
  pkg {
    repo = rootProject.group
    name = moduleName
  }
}
